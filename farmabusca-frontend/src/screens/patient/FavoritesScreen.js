import React,{useCallback,useState} from 'react'; import {View,Text,FlatList,StyleSheet,RefreshControl} from 'react-native'; import {useFocusEffect} from '@react-navigation/native'; import MedicineCard from '../../components/MedicineCard'; import {EmptyState,ErrorState,LoadingSkeleton} from '../../components/ScreenState'; import {getFavorites,removeFavorite} from '../../services/api'; import {colors,spacing,typography} from '../../theme';
export default function FavoritesScreen({navigation}){const[items,setItems]=useState([]);const[loading,setLoading]=useState(true);const[refreshing,setRefreshing]=useState(false);const[error,setError]=useState('');const load=useCallback(async(refresh=false)=>{refresh?setRefreshing(true):setLoading(true);setError('');try{const res=await getFavorites();setItems((res.data||[]).map(f=>({...f.Medicine,favoriteId:f.id})).filter(x=>x.id));}catch(e){setError(e.response?'Não foi possível carregar os favoritos.':'Sem ligação à Internet.')}finally{setLoading(false);setRefreshing(false)}},[]);useFocusEffect(useCallback(()=>{load();},[load]));const remove=async id=>{try{await removeFavorite(id);setItems(old=>old.filter(x=>x.id!==id));}catch(e){setError('Não foi possível remover o favorito.')}};return <View style={styles.container}><View style={styles.header}><Text style={styles.title}>Favoritos</Text><Text style={styles.subtitle}>Medicamentos guardados para consultar rapidamente.</Text></View>{loading?<View style={styles.pad}><LoadingSkeleton rows={4}/></View>:error&&!items.length?<ErrorState message={error} onRetry={load}/>:<FlatList data={items} keyExtractor={x=>String(x.id)} contentContainerStyle={styles.list} refreshControl={<RefreshControl refreshing={refreshing} onRefresh={()=>load(true)} tintColor={colors.primary}/>} renderItem={({item})=><MedicineCard item={item} favorite onFavorite={()=>remove(item.id)} onPress={()=>navigation.navigate('MedicineDetails',{item})}/>} ListEmptyComponent={<EmptyState title="Ainda não guardou medicamentos" message="Toque no coração de um medicamento para encontrá-lo aqui." icon="heart-outline" actionLabel="Pesquisar medicamentos" onAction={()=>navigation.navigate('Pesquisar')}/>}/>}</View>}
const styles=StyleSheet.create({container:{flex:1,backgroundColor:colors.background},header:{paddingHorizontal:spacing.xl,paddingTop:spacing.xl},title:{...typography.title,color:colors.text},subtitle:{...typography.body,color:colors.textSecondary,marginTop:4},list:{padding:spacing.xl,paddingBottom:40,flexGrow:1},pad:{padding:spacing.xl}});
